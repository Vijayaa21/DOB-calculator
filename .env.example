# Server Configuration
SERVER_PORT=3000

# Database Configuration
# Adjust DB_PORT if your local PostgreSQL uses a different port (common on Windows: 5433)
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=dob_calculator
DB_SSLMODE=disable
